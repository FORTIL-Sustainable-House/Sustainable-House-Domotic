//document.getElementById("log").innerHTML = temperature
//document.getElementById("log").innerHTML = timestamps

//legend = '# of Votes';
//labels = [timestamps[0], timestamps[timestamps.length - 1]];
values = new Array();
for (i = 0; i < timestamps.length; i++) {
   console.log(timestamps[i] + " - " +  temperature[i]);
   values.fill({x: timestamps[i], y: temperature[i]})
}
document.getElementById("log").innerHTML = values + temperature.length


var ctx = document.getElementById('myChart');
/*var chart = new Chart(ctx, {
    type: 'line',
    data: {
        labels: timestamps,
        datasets: [{
            label: legend,
            data: temperature,
            backgroundColor: 'rgba(255, 99, 132, 0.2)',
            borderWidth: 1
        }]
    },
    options: {
        scales: {
            yAxes: [{
                ticks: {
                    beginAtZero: true
                }
            }],
        }
    }
});*/


function newDate(days) {
            yolol = moment().add(days, 'd').toDate();
            console.log(yolol);
            return yolol;
        }

function newDateString(days) {
            yolol = moment().add(days, 'd').format();
            console.log(yolol);
            return yolol;
        }


var myChart = new Chart(ctx, {});
